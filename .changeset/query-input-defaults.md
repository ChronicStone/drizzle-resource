---
"drizzle-resource": patch
"@drizzle-resource/core": patch
---

Normalize omitted pagination, sorting, filters, and search fields at the query engine boundary so valid sparse transport requests do not fail during list or summary queries.
