---
"drizzle-resource": patch
"@drizzle-resource/core": patch
---

Export the typed resource returned by `defineResource` so applications can type shared resource helpers with views, model policies, and summaries.
